typ status = void
typ _ = char
typ _ = int
typ _ = bool
typ _ = status

typ array = [none] void
typ ptr = ^int
typ person = (  { name : ^char, age : int} )

var i : stevilo
var j : bool

fun compute() : void
fun sum(a : ^char, b : int) : int
    = 42

